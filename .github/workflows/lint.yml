name: Lint Code Base
on:
  push:
    branches:
      - main

  pull_request:
    branches:
      - main

jobs:
  build:
    name: Lint Code Base
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: read
      statuses: write
    steps:
      - name: Checkout Code
        uses: actions/checkout@v7
        with:
          # Full git history is needed to get a proper
          # list of changed files within `super-linter`
          fetch-depth: 0
      - name: Lint Code Base
        uses: super-linter/super-linter/slim@v8
        env:
          FILTER_REGEX_EXCLUDE: .*yarn\.lock.*
          # Examples are standalone projects: their imports only resolve with
          # their own node_modules installed (plus bundler path aliases), which
          # the root linter never has. Each example verifies itself via its own
          # install/build/lint, so skip import-resolution linting here while
          # keeping formatting and every other linter active on examples/.
          TSX_FILTER_REGEX_EXCLUDE: ^examples/
          TYPESCRIPT_ES_FILTER_REGEX_EXCLUDE: ^examples/
          VALIDATE_ALL_CODEBASE: false
          VALIDATE_TYPESCRIPT_STANDARD: false
          VALIDATE_JSCPD: false
          VALIDATE_BIOME_FORMAT: false
          VALIDATE_BIOME_LINT: false
          VALIDATE_CHECKOV: false
          VALIDATE_GITHUB_ACTIONS_ZIZMOR: false
          VALIDATE_TRIVY: false
          VALIDATE_ARM: false
          VALIDATE_CLOUDFORMATION: false
          VALIDATE_DOCKERFILE_HADOLINT: false
          VALIDATE_KUBERNETES_KUBEVAL: false
          VALIDATE_LATEX: false
          VALIDATE_NATURAL_LANGUAGE: false
          VALIDATE_POWERSHELL: false
          VALIDATE_TERRAFORM_TFLINT: false
          VALIDATE_TERRAFORM_TERRASCAN: false
          DEFAULT_BRANCH: main
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
